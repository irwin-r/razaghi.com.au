import PropTypes from "prop-types";

// eslint-disable-next-line import/prefer-default-export
export const FontAwesomeIconPropShape = PropTypes.oneOfType([
  PropTypes.object,
  PropTypes.array,
  PropTypes.string,
]);
